# Converts a matrix into a string in style of .cal files

Converts a matrix into a string in style of .cal files

## Usage

``` r
matrix_cal_string(object)
```

## Arguments

- object:

  matrix to be converted into string

## Value

character of length 1, where each row is separated by ';' and all values
in each row is separated by ','
