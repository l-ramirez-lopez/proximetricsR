% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_property_names.R
\name{extract_property_names}
\alias{extract_property_names}
\title{Extract the property names from a given \code{data.frame}}
\usage{
extract_property_names(x)
}
\arguments{
\item{x}{a \code{data.frame}, as normally obtained by
\code{\link{proximate_read_data}}, \code{\link{read_spc}},
\code{\link{proxiscout_read_data}}, or some other data parsing function.}
}
\value{
A character vector, containing only the names of numerical properties.
If no property names were identified, return a character vector of length 0.
}
\description{
This function aims to extract the column names of properties from \code{x}. A property
in this context is a response vector of numerical values that then later can
be calibrated for predictions (such as with \code{\link{calibrate}}).
}
\details{
Depending on the \code{class} of \code{x},  the names of the properties are identified
differently. For all cases, only columns which contain numerical values
(including \code{NA}) are considered as potential properties.

If \code{x} is of class \code{proximate_data}, the property names are identified as follows:
\itemize{
\item{}{Located between columns "Reference" and "Begin".}
\item{}{Not named according to any of the following names: "ROW", "Check",
"Date", "SNR", "SRN", "ID", "Barcode", "Note", "Result", "Reference",
"Begin", "End", "Recipe", "Composition", "Images", "spc".}
\item{}{Contain only numerical values (including NA).}
}

If \code{x} is of class \code{proxiscout_data}, property names are identified as columns that
contain only numerical values (including \code{NA}) and are not matched by any of the
following, case-insensitive regex (each wrapped by \code{^} and \code{$}):
\itemize{
\item{}{\code{id}}
\item{}{\verb{sample[_. ]?name}}
\item{}{\verb{captured[_. ]?at}}
\item{}{\verb{device[_. ]?id}}
\item{}{\verb{created[_. ]?(by|at)}}
\item{}{\verb{on[_. ]?behalf[_. ]?of}}
\item{}{\verb{lot[_. ]?name}}
\item{}{\verb{scanner([_. ]?id)?}}
\item{}{\verb{original[_. ]?value}}
\item{}{\verb{display[_. ]?value}}
\item{}{\code{note}}
\item{}{\code{location}}
\item{}{\code{supplier}}
\item{}{\code{device}}
\item{}{\code{spc}}
\item{}{\code{predictions}}
\item{}{\code{.repetition_group} (internal column added by
\code{\link{proxiscout_read_data}})}
}

If \code{x} is of neither class, all columns with numerical values are considered to be properties
}
